import { type Config } from "prettier";

const config: Config = {
  printWidth: 80,
  tabWidth: 4,
  trailingComma: "all",
  singleQuote: false,
  semi: true,
  bracketSpacing: true,
};

export default config;
